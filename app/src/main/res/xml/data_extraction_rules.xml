<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ companion to allowBackup="false": exclude every app file from cloud backup
     and device-to-device transfer, so the user dictionary / bigrams / per-app language never
     leave the device (PRIVACY.md). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
